import { redirect } from 'next/navigation'

// Executive Summary is temporarily disabled — focus is on the Financial Statements page.
export default function ExecutivePage() {
  redirect('/financials')
}
